# Moshly Hub Environment Variables
# Copy to .env for local wrangler development

# JWT Secret for signing session tokens
# WARNING: Must be identical across Hub (Cloudflare) and Spokes (Fly.io)
JWT_SECRET=dev_secret_moshly

# D1 Database ID (Bound in wrangler.toml)
MOSHLY_DB_ID=078bb103-ae9b-4975-9e21-66877f480333
