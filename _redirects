# 1. Force Clean URLs (Strip .html from the browser bar)
# 2. i'm commenting out this command - /*.html  /:1  301

# 3. Cloudflare handles mapping clean URLs to .html files automatically if "Pretty URLs" is enabled.
# We only need explicit redirects here for non-standard mappings or 301s.

/pricing            /pricing.html            200
/login              /login.html              200
/signup             /signup.html             200
/dashboard          /dashboard.html          200
/admin              /admin.html              200
/index              /                        301
/fifthsense         /apps/fifthsense.html    200
/quote              /apps/quote.html         200
